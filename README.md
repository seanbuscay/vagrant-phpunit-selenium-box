vagrant-phpunit-selenium-box
============================

Provides a Vagrant Box to run automated (headless) web browser tests, using the Selenium WebDriver and tests written as PHPUnit_Selenium Test Cases.
